// The size of the grid
export const GRID_SIZE = 50;
// How many consecutive numbers should be found
export const CONSECUTIVE_COUNT = 5;
// Only consecutive AND sorted numbers are allowed
export const ONLY_SORTED_NUMBERS = true;